<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.2/css/bulma.min.css">
  <title>Watch Store - Login</title>
  <style>
    body {
      background-image: url('https://images.unsplash.com/photo-1522312346375-d1a52e2b99b3');
      background-size: cover;
      background-position: center;
      height: 100vh;
    }

    .login-box {
      margin-top: 10%;
    }

    .card {
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      border-radius: 15px;
    }

    .button.is-primary {
      background-color: #00d1b2;
      border-color: transparent;
    }

    .button.is-primary:hover {
      background-color: #00b89c;
    }

    .hero.is-primary {
      background-color: rgba(255, 255, 255, 0.8);
      border-radius: 10px;
    }
  </style>
</head>

<body>
  <section class="hero is-fullheight">
    <div class="hero-body">
      <div class="container">
        <div class="columns is-centered">
          <div class="column is-4-desktop is-6-tablet">
            <div class="box login-box">
              <h1 class="title has-text-centered">Watch Store</h1>
              <h2 class="subtitle has-text-centered">Login to your account</h2>
              <% if (err) { %>
               <p><%= err %></p>
              <% } %>
               
              <form action="" method="POST">
                <div class="field">
                  <label class="label">User Name</label>
                  <div class="control has-icons-left">
                    <input class="input" name="username" type="text" placeholder="Enter your User Name" required>
                    <span class="icon is-small is-left">
                      <i class="fas fa-envelope"></i>
                    </span>
                  </div>
                </div>

                <div class="field">
                  <label class="label">Password</label>
                  <div class="control has-icons-left">
                    <input class="input" name="password" type="password" placeholder="Enter your password" required>
                    <span class="icon is-small is-left">
                      <i class="fas fa-lock"></i>
                    </span>
                  </div>
                </div>

                <div class="field">
                  <label class="checkbox">
                    <input type="checkbox">
                    Remember me
                  </label>
                </div>

                <div class="control">
                  <button class="button is-primary is-fullwidth" type="submit">Login</button>
                </div>
              </form>
              <p class="has-text-centered">
                <a href="#">Forgot your password?</a>
              </p>
              <p class="has-text-centered">
                <a href="/users/register">Don't have an account? Sign up here</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FontAwesome for Icons -->
  <script src="https://kit.fontawesome.com/a076d05399.js"></script>
</body>

</html>
